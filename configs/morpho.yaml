# Morpho Blue 配置 —— M0 已核实（2026-08-05，见 deployments/M0-chain-facts.md）
# 重要结论：Robinhood 上 Morpho Blue 已部署（区块 286）但从未创建市场（CreateMarket 事件 = 0 次）。
# Morpho API 的 chainId=4663 数据与链上状态不符，不可信。
# M6-M8（清算）推迟：等真实市场出现（监听 CreateMarket 事件）后再启用。
morpho:
  blue: "0x9d53d5e3bd5e8d4cbfa6db1ca238aea02e651010" # verified Morpho Blue (v0.8.19)，deploy block 286
  bootstrap_api: "https://blue-api.morpho.org/graphql" # 仅作参考，必须链上交叉验证
  markets: [] # 链上无市场；出现后在此登记（loan/collateral/oracle/irm/lltv）
